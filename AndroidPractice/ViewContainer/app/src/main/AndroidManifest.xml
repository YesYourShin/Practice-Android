<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sdh.viewcontainer">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/yju_logo"
        android:label="영진 웹브라우저"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".TabHostPractice2"></activity>
        <activity android:name=".TabHostPractice" />
        <activity android:name=".SlidingDrawerPractice" />
        <activity android:name=".ViewFlipperPractice" />
        <activity android:name=".SlidingLayoutPractice2" />
        <activity android:name=".LayoutViewFlipper" />
        <activity android:name=".MainActivity" />
        <activity android:name=".WebViewPractice"
            android:label="나의 웹브라우저"
            android:logo="@drawable/yju_logo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>