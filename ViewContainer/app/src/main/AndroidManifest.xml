<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sdh.viewcontainer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ViewContainer">
        <activity android:name=".TabHostPractice"></activity>
        <activity android:name=".SlidingDrawerPractice" />
        <activity android:name=".ViewFlipperPractice" />
        <activity android:name=".SlidingLayoutPractice2" />
        <activity android:name=".LayoutViewFlipper" />
        <activity android:name=".MainActivity" />
        <activity android:name=".TabHostPractice2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>